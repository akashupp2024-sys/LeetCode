class Solution {
public:
    vector<int> asteroidCollision(vector<int>& asteroids) {

        vector<int< st;
        for(int a : asteroid){
            
            
            while(s.size() > 0 && s.back() > 0 && a < 0  && s.back() < abs(a)){
                s.pop_back();


                if(){

                }

                else if (){
                    st.push_back(a);
                }
                else {
                    
                }
            }

        }
        
        
    }
};